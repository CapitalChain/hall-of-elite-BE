# =============================================================================
# Hall of Elite Backend - Environment Variables
# =============================================================================
# Copy to .env and fill in values. Use .env for LOCAL; set these in your
# deployment platform (e.g. Vercel, Railway, server) for PRODUCTION.
# =============================================================================

NODE_ENV=development
PORT=6200

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
# LOCAL: use your existing local DB (e.g. capitalchain_mt5)
# PRODUCTION: use server DB — database: hall_of_elite, user: hall_user
# -----------------------------------------------------------------------------

# Local example (default for development):
DATABASE_URL=postgresql://ixbspartan@localhost:5432/capitalchain_mt5?schema=public

# Production example (set DATABASE_URL to this on your server / hosting env;
# replace YOUR_SERVER_HOST with your PostgreSQL host, e.g. db.example.com or IP):
# DATABASE_URL=postgresql://hall_user:Success2025@YOUR_SERVER_HOST:5432/hall_of_elite?schema=public

# -----------------------------------------------------------------------------
# App
# -----------------------------------------------------------------------------
# Allowed CORS origin (primary frontend URL). In production set to your frontend (e.g. https://hall.capitalchain.co).
# When NODE_ENV=production, https://hall.capitalchain.co is always allowed in addition to CORS_ORIGIN.
CORS_ORIGIN=http://localhost:6100
# Optional: comma-separated additional origins
# CORS_ORIGINS=https://hall.capitalchain.co,http://localhost:3000
JWT_SECRET=your-secret-key-min-32-chars
JWT_EXPIRES_IN=7d

# -----------------------------------------------------------------------------
# Rate limiting (optional; defaults: auth 10/15min, API 200/15min per IP)
# -----------------------------------------------------------------------------
# RATE_LIMIT_AUTH_MAX=10
# RATE_LIMIT_AUTH_WINDOW_MS=900000
# RATE_LIMIT_API_MAX=200
# RATE_LIMIT_API_WINDOW_MS=900000
